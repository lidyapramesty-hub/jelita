# Database (PostgreSQL - use Vercel Postgres, Neon, or Supabase for production)
DATABASE_URL="postgresql://user:password@host:5432/jelita_backend"

# JWT Secret (generate a strong random string for production)
JWT_SECRET="your-secret-key-here"
